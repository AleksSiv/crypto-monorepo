# Crypto Monorepo

This repository contains:

- `backend/` - Node.js + Fastify + Redis + Binance WebSocket API
- `frontend/` - Next.js + Tailwind CSS dashboard

Use `docker-compose` to run everything locally.
